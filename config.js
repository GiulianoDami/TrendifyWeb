
// API keys should be kept secure in production
// For demo purposes, these are shown but should be replaced with actual keys
export const TWITTER_API_KEY = 'YOUR_TWITTER_BEARER_TOKEN';
export const NEWS_API_KEY = 'YOUR_NEWS_API_KEY';

// Note: In a real application, these keys would be stored in environment variables
// and never committed to version control. This is a demo placeholder only.